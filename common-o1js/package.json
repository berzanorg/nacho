{
  "name": "common-o1js",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "rm -rf build/ && tsc && node --test build/*.test.js",
    "build": "rm -rf build/ && tsc && npm pack --pack-destination build/"
  },
  "dependencies": {
    "o1js": "^0.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
}